import React from 'react';
import styles from './index.less';

const FontColorPanel = React.memo(() => {
    return <div className={styles.fontColorPanel}>FontColorPanel</div>;
});

export default FontColorPanel;
